const lightbox=document.querySelector('[data-lightbox]');const lightboxImage=document.querySelector('[data-lightbox-image]');const lightboxTitle=document.querySelector('[data-lightbox-title]');const lightboxMeta=document.querySelector('[data-lightbox-meta]');const closeButton=document.querySelector('[data-close-lightbox]');function openLightbox(src,title,meta){lightboxImage.src=src;lightboxImage.alt=title||'';lightboxTitle.textContent=title||'';lightboxMeta.textContent=meta||'';lightbox.classList.add('is-open');document.body.style.overflow='hidden'}function closeLightbox(){lightbox.classList.remove('is-open');lightboxImage.src='';document.body.style.overflow=''}document.querySelectorAll('[data-lightbox-trigger]').forEach(t=>{t.addEventListener('click',e=>{e.preventDefault();openLightbox(t.href,t.dataset.title,t.dataset.meta)})});closeButton?.addEventListener('click',closeLightbox);lightbox?.addEventListener('click',e=>{if(e.target===lightbox)closeLightbox()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&lightbox?.classList.contains('is-open'))closeLightbox()});